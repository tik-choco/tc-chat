# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# mistlib's own git clone cache used by scripts/fetch-mistlib.mjs; the
# built output it produces (src/vendor/mistlib) IS committed.
.mistlib-src
# same, but for the standalone repo that now holds the JS wrapper
# (wrappers/web) since mistlib split it out of the engine repo.
.mistlib-examples-src

# Rust build output (cli/)
target

.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.omc
CLAUDE.md
